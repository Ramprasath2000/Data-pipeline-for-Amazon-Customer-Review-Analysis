Data cleaning:

step1: We extracted the raw datas which contains 150 records regarding amazon product reviews based on three categories.

step2: We cleaned the datas, eg. removing unwanted spaces, normalizations, removing null values etc.

step3: Then we loaded the datas into mysql using sqlalchemy into TIDB server.

step4: We performed some queries on those loaded datas.

step5: We visualized those queries using power Bi.
